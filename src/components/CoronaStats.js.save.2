cimport React from "react";

function CoronaStats(props){
    return (
        <div>
            {props.countries.map(c => <p>{c.totaldeaths}</p>) }
        </div>
    );
}

export default CoronaStats;
